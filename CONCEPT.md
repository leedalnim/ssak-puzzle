# 슥삭퍼즐 — 컨셉 정의서

> 상태: **컨셉·파이프라인 확정** — 목업 완성 단계 진행 중

## 한 줄 컨셉

무기력하게 지내던 청년 여성이 **방 한 칸을 닦는 것**을 계기로,
알바에 도전하며 **다시 세상 밖으로** 나아가는 한붓그리기 청소 퍼즐.

- 주인공: **청년 여성**
- 태그라인: *무기력한 방 한 칸에서, 다시 세상 밖으로*

---

## 1. 서사 — 장소 4곳 × 3스테이지 (총 12)

**장소마다 5×5 → 6×6 → 7×7 로 커진다.** 같은 장소를 세 번 겪으며 익숙해지고,
다음 장소로 넘어가면 다시 5×5부터 시작해 부담 없이 확장된다.

| 막 | 장소 | 스테이지 | 크기 |
|---|---|---|---|
| 1 | 자취방 | 내 방 · 침대 옆 · 쌓인 빨래 | 5×5 → 6×6 → 7×7 |
| 2 | 편의점 | 편의점 첫날 · 진열대 사이 · 마감 청소 | 5×5 → 6×6 → 7×7 |
| 3 | 카페 | 카페 오픈 · 테이블 사이 · 마감 후 | 5×5 → 6×6 → 7×7 |
| 4 | 베이커리 | 오픈 전 · 작업대 아래 · 마지막 밤 | 5×5 → 6×6 → 7×7 |

감정선: **무기력 → 몸이 움직임 → 문 밖이 궁금해짐 → 사람들 사이로 → "꽤 멀리 왔구나"**
해금: 구겨진 잠옷 → 노란 고무장갑 → 편의점 유니폼 → 운동화 → 바리스타 앞치마 → 제빵사 모자

> 배경은 현재 자취방 1종뿐. 편의점·카페·베이커리 배경이 확보되면 `bg` 필드만 바꾸면 된다.

### 격자 생성 — 구성적 보장 (`tools/gen_stages.py`)
무작위 배치 후 솔버로 거르는 방식은 **격자가 커지면 사실상 못 찾는다.**
한붓그리기는 체스판 패리티 제약(흑·백 칸 합이 맞아야 함)이 강해 대부분의 배치가 안 풀린다.

→ **경로를 먼저 만들고 방문 횟수를 더러움으로 삼는다.** 그 경로를 재생하면 반드시 클리어되므로
풀림이 구성적으로 보장되고, 검증도 O(n) 재생으로 즉시 끝난다.
(전수 탐색 솔버 `solve.py`는 7×7에서 사실상 못 끝냄)

---

## 2. 화면

- **세로 9:16** (모바일 / 앱인토스)
- 현재 구현은 가로 688×538 → **전환 필요**
- 구성: 상단 HUD(스테이지·시간·남은 칸) / 중앙 보드 / 하단 바(힌트·되돌리기)

---

## 3. 아트 방향 — **탑다운 3D 클레이** (확정)

- **미니어처 클레이 디오라마** 느낌. 매트한 점토 재질, 부드러운 그림자, 둥근 모서리
- 카메라: **거의 위에서, 살짝 기울임** — 격자는 직사각 유지
  - **약한 원근은 유지** (아래쪽 타일이 조금 큰 것이 더 입체적이라 채택)
- 팔레트: 크림·아이보리 + 밝은 우드 + 세이지 그린 포인트, 밝은 자연광
- 캐릭터: 2등신 치비, 대걸레 들고 서 있는 자세

### 검토 후 폐기한 방향
| 방향 | 폐기 사유 |
|---|---|
| 픽셀아트 | 3D 클레이가 귀여움에서 확실히 우위 |
| 아이소메트릭 45° | 마름모 격자 → 좌표계 전면 수정 필요, 오클루전 발생 |

---

## 4. 구조 원칙 ⭐ 가장 중요

```
[배경 1장]   가구·소품 포함, 가운데 바닥은 비어 있음   ← AI 생성 (통짜)
     +
[코드]       보드 격자 · 타일 · 캐릭터 · HUD · 이펙트   ← 그리드 크기 자유
```

**이유**: 스테이지마다 그리드가 3×3~5×6으로 다름. 배경에 격자가 박혀 있으면
그리드마다 배경을 새로 만들어야 함. 분리하면 배경 1장으로 모든 그리드 대응.

**부가 효과**: 스프라이트 시트 슬라이싱이 완전히 사라짐 (이번 작업의 최대 병목).

### UI 구성 (목업 확정 — B안)
- **상단(슬림)**: 시간 pill · 남은 칸 pill · 일시정지 버튼
  (STAGE 번호는 인트로에서만 — 플레이 중엔 불필요)
- **하단**: 도움말(?) · **되돌리기(가장 크게, 가운데)** · 다시하기(⟳)
  - 한붓그리기는 막히면 되돌리는 게임 → UNDO가 최다 사용 버튼
  - 아이템 슬롯은 시스템 구현 후 확장(C안)
- **`?` = 가이드 모달** — 더러움 5단계 범례(= n번 밟기)와 규칙 안내
  - 타일 위 도트마커를 **넣지 않는 대신** 이 모달이 학습 역할을 함
- 스타일: 밝은 크림 패널 + 밝은 우드 테두리

### 타일 규칙
- 더러움 **5단계** (1~5) + 깨끗(0) = 총 6상태
- 보드 위 분포는 **자연스럽게 흩어지게** — 단계순 정렬(색견본표처럼)은 금지
- 대부분 옅고, 2~3칸만 심하게

### 캐릭터 — 2등신 치비 (확정)
- **2등신 치비**: 큰 둥근 머리 + 작은 몸통
- 갈색 머리(messy bun), 흰 티셔츠 + 초록 멜빵바지, 흰 운동화
- **대걸레를 들고 서서 미는 자세** (쪼그려 앉는 포즈는 폐기 — 이동이 어색)
- 크기: **한 칸에 들어가는 정도**
- 스프라이트 시트: **4방향(하/상/좌/우) × 3프레임 = 4행 3열 균일 그리드**
  - 배경 **마젠타(#FF00FF)** 단색 → 배경 제거 100% 깨끗
  - **텍스트·라벨·격자선 금지** (지난 시트 재단 실패 원인)

### 타일 표시
- 타일 위 **도트마커 없음** — 더러움 정도(색·질감)만으로 단계 표현
- 대신 **`?` 가이드 모달**에 범례 제공
- 5단계는 **색 대비를 확실히 벌려** 한눈에 구분되게 (1=거의 깨끗 ↔ 5=짙은 갈색)
- 숫자 오버레이는 코드로 그리므로, 필요해지면 나중에 켤 수 있음

### 보드 배치 규칙
- 보드 **영역은 고정**, **셀 크기가 가변** (`cell = 영역폭 / cols`)
- 셀 최대 크기 제한 후 가운데 정렬 (3×3에서 셀이 과하게 커지지 않도록)
- 보드 폭: 화면 폭의 **78~85%** 권장
  (컨셉 시안은 65% — 플레이 영역이 좁음. 단, 방이 보여야 서사가 살아서 100%는 지양)

---

## 5. 톤 & 무드

**밝고 산뜻한 톤 유지** — 크림/아이보리 베이스, 밝은 내추럴 우드(주황빛 아님),
세이지 그린·민트 포인트, 밝은 자연광.

> ⚠️ 초기 기획의 "어두운 보라(#241d2b)로 시작" 무드는 **폐기**.
> 어두우면 모바일에서 가독성이 떨어지고 보기에도 좋지 않음.
> 대신 **채도·따뜻함의 변화**로 성장을 표현 (차분한 방 → 화사한 베이커리).

---

## 6. 에셋 계획 & 크레딧 예산

가용 크레딧: **10** (힉스필드 free) · 단가: 1K = 1.25cr, 2K = 2cr

### 역할 분담

| 담당 | 내용 |
|---|---|
| **사용자** | GPT Image 2로 **컨셉 이미지** 생성 = 스타일 기준 확정 |
| **Claude** | **힉스필드로 실제 에셋 생성** → 가공 → 코드 통합 |

| 항목 | 방식 |
|---|---|
| 배경 6장 | **GPT 생성** (자취방 ✅ / 나머지 5장 대기) |
| 타일 6종 | **GPT 생성 → 분리 완료** ✅ |
| 장애물 6종 | **GPT 생성 → 분리 완료** ✅ |
| 캐릭터 2등신 치비 4방향×3프레임 | **GPT 생성 → 누끼·정리 완료** ✅ |
| UI 패널·버튼 6종 | **GPT 생성 → 분리 완료** ✅ |
| 가이드 모달 · 이펙트 | **코드** |

프롬프트는 [`PROMPTS.md`](PROMPTS.md) 참조.

**힉스필드 = 에셋 생성 도구** (크레딧 10). ⚠️ 현재 MCP 연결 끊김 — 생성 전 재연결 필요.
**충전 주기 미확인** — 재연결 시 `transactions`로 확인해 여기 기록할 것.



---

## 6.5 작업 파이프라인 (확정)

```
1. 사용자 : GPT Image 2로 컨셉 이미지 = 스타일 기준        ✅ 완료 (클레이 톤 확정)
2. Claude : 힉스필드로 그 스타일대로 실제 에셋 생성
3. Claude : 가공 후 게임에 적용
```

> 컨셉 이미지는 Claude가 **보기만** 하면 되므로 붙여넣기로 충분.
> 실제 에셋은 Claude가 직접 생성하므로 **파일 전달 문제가 없음.**

### 생성 원칙 — 최대한 모아서 뽑는다
크레딧 절약을 위해 여러 에셋을 **한 시트에 묶어** 생성한다.

| 시트 | 내용 | 배치 |
|---|---|---|
| 타일·장애물 시트 | 타일 6종(clean+1~5) + 장애물 3종 | 균일 격자, 마젠타 배경 |
| 캐릭터 시트 | 4방향(하·상·좌·우) × 3프레임 | 4행 3열 균일 격자, 마젠타 배경 |
| 배경 | 장소별 1장 | 통짜, 가운데 빈 바닥 |

시트 공통 규칙: **균일한 셀 크기 · 마젠타(#FF00FF) 배경 · 텍스트/라벨/격자선 금지**
(과거 시트 재단 실패의 원인이 라벨과 불균일 여백이었음)

### 크레딧 배분 (1K = 1.25cr 기준)

| 차수 | 생성물 | 장수 | 비용 |
|---|---|---|---|
| 1차 | 자취방 배경 + 타일·장애물 시트 + 캐릭터 시트 | 3 | 3.75cr |
| — | 1차 결과를 게임에 붙여 스타일·정합 **검증** | | |
| 2차 | 나머지 배경 5장(부엌·현관·편의점·카페·베이커리) | 5 | 6.25cr |
| | **합계** | **8** | **10cr** |

- 1차 3장이면 **스테이지 1~3(자취방)을 완전히 플레이 가능**하게 만들 수 있음
- 재시도 여유가 없으므로 **1차 검증 후** 2차 진행
- 타일을 코드로 그리면 1.25cr 절약 가능 (클레이 질감은 저하)

### 용량 전략 (실측 기준)

| 형식 | 배경 1장 | 6장 합계 |
|---|---|---|
| PNG | 591 KB | 3.47 MB |
| **WebP q82** | **62 KB** | **0.37 MB** |
| WebP q82 + 720px | 43 KB | 0.25 MB |

> 실측은 단순 합성 이미지 기준. 실제 3D 렌더는 더 크지만 절감 비율(80~90%)은 유사.
> 현실적 예상: PNG 2MB → WebP 250KB.

1. **WebP 변환** — 3D 렌더처럼 그라데이션 많은 이미지에 특히 효과적
2. **적정 해상도** — 1080×1920이면 충분 (부드러운 스타일이라 확대해도 티 안 남)
3. **지연 로딩** — 스테이지 진입 시 해당 배경만 로드 → 초기 ~400KB
4. **캐릭터는 시트 1장** — 개별 12장보다 작음

예상 최종: **초기 로딩 ~400KB / 전체 ~2MB** (모바일 웹 게임으로 가벼운 편)

---

## 6.6 배경 실측값 (자취방) ✅

`assets/room/bg_studio.webp` — 1024×1536, **142KB** (원본 PNG 2.18MB → 6.4%, WebP q88)
원본 보관: `assets/concept/bg_studio_src.png`

### 보드 배치 좌표 (배경 1024×1536 기준) — **원근 사다리꼴**

바닥에 놓인 느낌을 위해 정사각형이 아닌 **사다리꼴**로 배치 (위가 좁음).

| 항목 | 값 |
|---|---|
| 상단 폭 | **680px** |
| 하단 폭 | **760px** (수축 10.5%) |
| y 범위 | **556 ~ 1256** (높이 700) |
| 가로 중심 | 화면 중앙 (x=512) |
| 화면 폭 대비 | 66~74% |

- 원근 강도는 **약함(10%)** 채택 — 바닥 나뭇결 소실 방향과 일치.
  중간(19%)·강함(27%)은 사다리꼴이 과해 타일이 찌그러져 보임.
- 코드에서 **호모그래피(단위 정사각형 → 사다리꼴)** 로 셀 좌표 계산.
  타일 에셋은 **평면 정사각형**으로 받아 코드가 변형 (이중 원근 방지).

가구를 전혀 가리지 않는 최대 정사각 영역. 좌우 여백 160px(책장·화분 회피),
상단 612px(침대·책상 아래), 하단 1312px(협탁·바구니 위).

### 그리드별 셀 크기 (`cell = min(700/cols, 700/rows)`)

| 그리드 | 셀 | 보드 크기 |
|---|---|---|
| 3×3 | 233 | 700×700 |
| 4×4 | 175 | 700×700 |
| 4×6 | 116 | 466×700 |
| 5×5 | 140 | 700×700 |
| 5×6 | 116 | 583×700 |
| 6×8 | 87 | 525×700 |

→ 모든 그리드가 같은 영역 안에 들어감. **영역 고정 + 셀 가변** 원칙 검증 완료.

---

## 6.7 캐릭터 에셋 완료 ✅

`assets/room/char/char_{down|up|left|right}_{0|1|2}.png` — 12프레임, 높이 280px 통일

- 원본: `assets/concept/char_sheet_src.png` (1536×1024, 3행×4열)
- 배경 `(226,226,226)` 중성회색 → **테두리 플러드필 + 중성색 판정**으로 제거
  (흰 티셔츠·운동화·대걸레는 외곽선 안쪽이라 보존됨)
- **흰 테두리(헤일로) 제거 필수 후처리**: 경계 픽셀 중 `min(RGB)>192 & 채도<24` 인
  밝은 중성색을 2패스로 제거 후 알파 0.5px 블러. 12프레임에서 2,760px 정리.
  (어두운 배경에 올려 검증할 것 — 숫자만으로는 흰옷과 구분 안 됨)
- 12컷을 연결요소로 자동 분리 후 방향별 재배치
  - `down` ← 정면 3컷 / `up` ← 뒷면 / `left` ← 측면 3/4
  - `right` ← **left 좌우반전** (게임 표준 방식)
- 셀 대비 스케일: **셀 높이 × 1.45**, 발이 셀 하단에 오도록 정렬

---

## 6.8 타일 · 장애물 에셋 ✅

### 타일 `assets/room/tiles/tile_0~5.png` (256×256)
GPT 생성 시트(마젠타 배경, 2행×3열) → 자동 분리. 6단계가 균등하게 진해짐.
> 코드 절차 생성본은 폐기 — AI 타일이 클레이 질감에서 우위.

### 장애물 `assets/room/obstacles/` (6종)
`obs_books` · `obs_stool` · `obs_basket_sq` · `obs_boxes` · `obs_plant` · `obs_basket_rd`
2장의 시트에서 각 3종씩 분리. 배치 스케일 = 셀 높이 × 0.85

### 마젠타 키잉 성공
`#FF00FF` 배경 지시가 완벽히 지켜져 **테두리 플러드필로 100% 깨끗하게 분리**.
(과거 시트 재단 실패의 해법이 검증됨)

### 배경 v2 — 고양이 추가
`assets/room/bg_studio.webp` (144KB). 침대 위 턱시도 고양이 추가.
초기 기획의 *"고양이 = 주인공의 유일한 동거 친구"* 설정과 연결됨.
픽셀 diff 결과 변경 영역은 침대(64~192, 320~448)뿐, 나머지는 동일.

---

## 6.9 렌더링 규칙 (폴리싱 확정)

`tools/preview_room.py` 가 기준 구현. 게임 캔버스도 동일 규칙을 따른다.

### 보드는 **통짜로 한 번만** 변형
```
평면에서 [나무프레임 + 타일격자] 조립  →  호모그래피로 1회 원근 변형
```
타일을 **개별로** 변형하면 이음새가 어긋난다. (실제로 겪은 문제)

### 오브젝트는 변형하지 않는다 + **밑면 기준 정렬**
장애물·캐릭터는 원근 변형 대상이 아님.

> ⚠️ 바운딩박스 중앙 정렬은 **공중에 뜬 것처럼** 보인다.
> **밑면(접지선)을 셀 중앙보다 살짝 아래(셀 높이 ×0.16~0.18)에 놓고 위로 세울 것.**
> 그러면 오브젝트 윗부분이 타일 위쪽으로 자연스럽게 삐져나온다.

크기 = 셀 높이 비례: 장애물 **×1.05**, 캐릭터 **×1.85**

### 그림자 3종
| 대상 | 방식 |
|---|---|
| 보드 | 사다리꼴을 3단(오프셋 26/16/8, α70/80/70)으로 깔고 블러 22 |
| 오브젝트·캐릭터 | **좁고 진한 코어**(밑면과 겹치게 위로 올림) + **넓고 옅은 확산** 2겹 |
| 타일 | **그림자 없음** ❌ 타일별 AO는 넣지 말 것 |

> ⚠️ 넓은 타원 하나만 깔면 오브젝트가 **둥둥 떠 보인다.**
> 밑면과 **겹치는 위치**에 좁고 진한 코어를 반드시 넣을 것.

> ⚠️ **타일마다 AO(아래쪽 그늘)를 넣지 말 것.** 모든 칸 하단에 회색 띠가 생겨
> 더러움처럼 보이고 보드 전체가 탁해진다. 그림자는 **오브젝트 접지 그림자만** 사용.

> 캔버스에서도 `ctx.filter='blur()'` 또는 방사형 그라데이션으로 동일 품질 재현 가능.

### 마젠타 디스필 (필수 후처리)
마젠타 배경 키잉 후 **가장자리에 분홍 잔상**이 남는다. 반드시 후처리할 것:
1. 알파 채널 `MinFilter`로 1~2px 침식
2. `(R+B)/2 - G > 26` 인 픽셀의 G를 끌어올려 스필 제거

타일·장애물 12개에서 총 2,243px 보정함.

---

## 6.10 UI 에셋 & 레이아웃 규칙 ✅

`assets/room/ui/` — GPT 생성(마젠타 배경) → 키잉·디스필 완료

| 파일 | 용도 |
|---|---|
| `hud_time.png` | 시간 패널 (시계 아이콘 내장) |
| `hud_tiles.png` | 남은 칸 패널 (타일 아이콘 내장) |
| `btn_pause.png` | 일시정지 (원형) |
| `btn_help.png` | 도움말 `?` (원형) |
| `btn_undo.png` | 되돌리기 (알약형, ↩ 아이콘 중앙) |
| `btn_restart.png` | 다시하기 (원형) |

### 레이아웃 = **앵커 기준** (고정 좌표 금지)
숫자 길이가 바뀌므로(`01:45`↔`5:00`, `12`↔`3`) 절대 좌표로 배치하면 깨진다.

```
상단   [시간]────────────────[남은칸][⏸]
       ↑좌측 앵커              우측 앵커↑
하단   [?]──────[되돌리기]──────[⟳]
       ↑좌측     ↑화면 중앙      우측↑
```

### 패널은 **9-slice**로 늘려 쓴다
좌·우 끝(아이콘 포함)은 고정, 가운데만 신축 → 내용 길이에 자동 대응.
CSS `border-image` 로 구현. 숫자·텍스트는 **코드로** 그린다.

### 폰트 — 자가 호스팅
`assets/fonts/NanumSquareRound{R,B,EB}.woff2` (+woff 폴백), 400/700/800 세 굵기.

```css
@font-face { font-family:'NanumSquareRound'; src:url('../assets/fonts/NanumSquareRoundR.woff2') format('woff2'), ...; font-weight:400 }
```

> ⚠️ **외부 CDN(jsdelivr) 링크는 폐기.** 네트워크 정책이나 CDN 장애로 차단되면
> 폰트가 통째로 시스템 기본으로 대체돼 화면이 달라진다(개발 샌드박스에서 실제로 발생).
> npm `@kfonts/nanum-square-round`에서 받아 레포에 포함. OFL-1.1로 재배포 허용.

### 미해결
`btn_undo` 는 화살표가 **정중앙**이라 "되돌리기" 텍스트와 겹침.
→ 아이콘이 **왼쪽에 치우친** 변형을 추가 생성하거나, 아이콘까지 코드로 그릴 것.

---

## 7. 미결 사항

- [x] ~~캐릭터 재활용 여부~~ → **2등신 치비로 신규 제작 확정**
- [x] ~~캐릭터 스프라이트~~ → **완료** (`assets/room/char/`, 4방향×3프레임)
- [ ] 아이템 시스템 설계(대걸레/스프레이/빗자루 효과) — 하단 UI 확장 시점
- [x] ~~세로 전환 해상도~~ → **1024×1536** (배경 기준)
- [ ] **힉스필드 무료 크레딧 충전 주기 확인** (일일/월간) — 재연결 시 `transactions`로 조회
- [ ] 힉스필드 MCP 재연결 (현재 끊김)
- [ ] 목업에서 수치 추출 → 보드 비율·타일 색상값·UI 색

---

## 8. 폐기 / 보류

- 픽셀 스프라이트 **시트 슬라이싱 파이프라인** — AI 생성 시트는 균일 그리드가 없고
  라벨 텍스트가 그림에 박혀 있어 재단 비용이 과다. **통짜 배경 방식으로 대체.**
- 기존 집청소 테마 에셋(`assets/home/`, 99개)은 커밋에 보존 — 필요 시 복구 가능.
